# cp .env.local.example .env.local — loaded by mise (see mise.toml [env]).
# Used by `mise run publish` / `mise run release` (GitHub Packages).
GITHUB_ACTOR=your-github-user
GITHUB_TOKEN=ghp_your_token_with_write:packages
